## 1. Vertical Slice — restore Ruff baseline for repository helper scripts

- [x] 1.1 Fix the currently reported `ruff check` and `ruff format --check` failures in repository-shipped helper scripts, validate both commands pass, and record the result in this change.
